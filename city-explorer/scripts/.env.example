# Server-side only. Never put these in the app .env: anything there with the
# EXPO_PUBLIC_ prefix is bundled into the app binary.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_KEY=<service role key from `npx supabase status`>
TFL_APP_KEY=<optional, from https://api-portal.tfl.gov.uk>
